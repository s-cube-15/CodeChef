#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t, a, b, c;
    cin >> t;
    while (t--)
    {
        cin >> a >> b >> c;
        if (a > 50)
        {
            cout << "A" << endl;
            /* code */
        }

        else if (b > 50)
        {
            cout << "B" << endl;
            /* code */
        }

        else if (c > 50)
        {
            cout << "C" << endl;
            /* code */
        }
        else
            cout << "NOTA" << endl;
    }

    return 0;
}