#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t, a, b, c;
    cin >> t;
    while (t--)
    {
        cin >> a >> b;
        int q[b];
        for (int i = 0; i < b; i++)
        {
            /* code */ cin >> q[i];
        }
        for (int i = 1; i <= a; i++)
        {
            /* code */ cout << i<<" ";
        }
        cout << endl;

        /* code */
    }

    return 0;
}